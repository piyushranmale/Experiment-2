#include <stdio.h>
#include <math.h>
int main() 
{
float hypt, height, base;
printf("Enter height:");
scanf("%f", &height);
printf("Enter base:");
scanf("%f", &base);
hypt = sqrt ((height*height )+( base*base));
printf("By pythagorous theorem hypotenuous is = %f", hypt);
return 0;
}
