#include <stdio.h>

int main() {
    int x = 2;
    int y = 2;
    int result = x - y - 2; // Using the number '2' again in the operation
    printf("6. Double Subtraction (2 - 2 - 2): %d\n", result);
    return 0;
}
