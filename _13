#include <stdio.h>

int main() {
    int base = 2; 
    int times = 2; 
    int product = 0;
    
    for (int i = 0; i < times; i++) {
        product += base; 
    }
    
    printf("Loop Multiplication (2 * 2): %d\n", product);
    return 0;
}
