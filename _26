#include <stdio.h>

int main() {
    float a, b, c, perimeter;
    printf("Enter sides a, b, c: ");
    scanf("%f %f %f", &a, &b, &c);
    perimeter = a + b + c;
    printf("Perimeter of triangle = %.2f", perimeter);
    return 0;
}
