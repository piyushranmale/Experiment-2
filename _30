#include <stdio.h>

int main() {
    float d1, d2, area;
    printf("Enter diagonals d1 and d2: ");
    scanf("%f %f", &d1, &d2);
    area = (d1 * d2) / 2;
    printf("Area of rhombus = %.2f", area);
    return 0;
}
