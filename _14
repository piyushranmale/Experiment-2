#include <stdio.h>


float divide_numbers(int a, int b) {
   
    return (float)a / b;
}

int main() {
    int x = 2;
    int y = 2;
    float quotient = divide_numbers(x, y);
    printf("Function Division (2 / 2): %.2f\n", quotient);
    return 0;
}
