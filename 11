#include <stdio.h>

int add_numbers(int a, int b) {
    return a + b;
}
int main() {
    int x = 2;
    int y = 2;
    int result = add_numbers(x,y);
    printf("Function Sum (2 +2): %d\n", result);
    return 0;
}
